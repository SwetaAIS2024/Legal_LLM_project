# Copilot instructions (DUMMY TEMPLATE)

This is a minimal placeholder file for AI coding agents. Replace with project-specific guidance.

- Purpose: quick onboarding notes for automated editors.
- Key folders: `Prev_work_done/` contains the processing pipeline (clue extraction, answer generation, excel update).
- Run smoke test: ensure `final_output.xlsx` and a single `ops1.pdf` exist; run `python Prev_work_done\\generate_clues.py` for one question.
- Important patterns: scripts use `SPRTION` as the delimiter and append outputs (clear files before full runs).

Notes:
- Keep this file short. If you need a full reference, replace with a curated set of run/debug instructions and examples drawn from `Prev_work_done/README.md`.

_this is a placeholder for project-specific instructions._

